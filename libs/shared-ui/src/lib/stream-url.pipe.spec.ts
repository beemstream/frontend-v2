import { StreamUrlPipe } from './stream-url.pipe';

describe('StreamUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new StreamUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
