/* eslint-disable */
declare namespace Twitch {
  export class Embed {
    constructor(elem: string | Element, settings: { width: number | string; height: number | string; channel: string; parent?: string[], theme?: 'dark' | 'light' | '' }) { }
  }
}

